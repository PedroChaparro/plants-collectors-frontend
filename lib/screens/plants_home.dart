import 'package:flutter/material.dart';

class PlanstHome extends StatelessWidget {
  const PlanstHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Plants Home'),
      ),
    );
  }
}
